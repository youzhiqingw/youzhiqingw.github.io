# 本地资源路径指南（Firefly 博客）

> 本博客是 **Firefly（基于 Astro 的静态博客）**，所有内容、图片、配置都在固定目录里。
> 改完文件后，**推送到 `githubio` 仓库（即 `youzhiqingw.github.io`）即自动部署**，无需手动构建。
> 本指南的目的：让你下次直接按"类型 → 本地路径"定位，不用再一个文件夹一个文件夹翻。

---

## 一、文章（Posts）

| 项目 | 说明 |
|---|---|
| 本地目录 | `src/content/posts/` |
| 已有文章 | `hello-world.md`（你的第一篇文章） |
| 字段注意 | frontmatter 里日期字段叫 **`published`**（不是 `pubDate`） |
| 新建文章 | `pnpm new-post` 或 `node scripts/new-post.js` |
| 删除文章 | 直接删 `src/content/posts/` 下对应 `.md` 文件 |

---

## 二、动态（Dynamics / 朋友圈）

| 项目 | 说明 |
|---|---|
| 本地目录 | `src/content/dynamic/` |
| 当前状态 | **测试动态已全部删除**（原有 4 个示例 `.md`` 已清空） |
| 新建动态 | `pnpm new-dynamic`（或 `pnpm new-d`） |
| 数据来源 | 构建时由 `src/content/dynamic/*.md` 自动生成 `/api/dynamic.json`，页面读取该接口 |
| 配置 | `src/config/dynamicConfig.ts`（含 apiUrl、每页数量、Memos 接入等） |

---

## 三、图片资源总表（最常找的就是这些）

| 类型 | 本地路径 | 对应配置位置 | 备注 |
|---|---|---|---|
| 赞赏码（微信） | `public/assets/images/sponsor/wechat.png` | `src/config/sponsorConfig.ts` → `methods` 微信项 `qrCode` | 已替换为你的新图 |
| 赞赏码（支付宝） | `public/assets/images/sponsor/alipay.jpg` | `src/config/sponsorConfig.ts` → `methods` 支付宝项 `qrCode` | **注意是 `.jpg`**（旧版是 `.png`，已改配置） |
| 站长头像 | `src/assets/images/avatar.avif` | `src/config/profileConfig.ts` → `avatar` | 换头像直接覆盖此文件 |
| Favicon（站点图标） | `public/favicon/`（含 `firefly-32/128/180/192.png` 及 `light/dark` 变体） | `src/config/siteConfig.ts` → `favicon[]` | 整套替换时改这里 + 替换文件 |
| Banner / 背景壁纸 | `src/assets/images/banner.avif`；或 `src/assets/images/DesktopWallpaper/`、`MobileWallpaper/` | `src/config/backgroundWallpaper.ts` → `mode` 与图片路径 | 当前用 `DesktopWallpaper/d1.avif` |
| 相册图片 | `public/gallery/<album-id>/`（如 `firefly-2026/1.avif`、`cover.avif`） | `src/config/galleryConfig.ts` → `albums[].id` | 残留模板图：`firefly-2026/`、`encrypted-test/`（相册配置已删，图片仍在） |
| 广告图 | `public/assets/images/ad/` | — | 原 `ad1.webp` 已被你删除 |
| 特效素材 | `public/assets/images/effects/sakura.png` | — | 模板自带樱花特效 |

> 放图片的通用规则：放进 `public/` 下的文件，访问路径就是 `/xxx/yyy.png`；放进 `src/assets/` 下的，在配置里写 `assets/xxx.png`（会被构建优化）。

---

## 四、配置中心：`src/config/` 目录速查

| 文件 | 管什么 |
|---|---|
| `siteConfig.ts` | 站点标题 / 副标题 / 描述 / `site_url`（RSS、sitemap、canonical 的唯一源头）/ favicon / 导航栏标题 |
| `profileConfig.ts` | 站长昵称、头像、社交链接（GitHub / Email / QQ / RSS） |
| `sponsorConfig.ts` | 打赏方式（支付宝 / 微信 收款码路径）、打赏者列表 |
| `galleryConfig.ts` | 相册列表（id 对应 `public/gallery/` 子目录） |
| `dynamicConfig.ts` | 动态页配置、数据源 |
| `navBarConfig.ts` | 导航栏菜单与「链接」下拉 |
| `backgroundWallpaper.ts` | 背景 / Banner 壁纸 |
| `commentConfig.ts` | 评论系统（giscus） |
| `friendsConfig.ts` | 友情链接页 |
| `booknavConfig.ts` | 书签导航 |

---

## 五、单页内容

| 页面 | 本地文件 |
|---|---|
| 关于页 | `src/content/spec/about.md` |
| 友情链接页 | `src/content/spec/friends.mdx` |

---

## 六、常用命令

```bash
pnpm dev            # 本地预览（http://localhost:4321）
pnpm build          # 生产构建（部署由 push 自动触发，一般不用手动跑）
pnpm new-post       # 新建文章
pnpm new-dynamic    # 新建动态
```

部署 = 把改动 `git push` 到 `githubio`（`youzhiqingw.github.io`）的 `master` 分支，GitHub Actions 会自动构建并发布。

---

## 七、本次已做的变更（备案）

- 赞赏码：替换为你提供的新图 —— 微信 `wechat.png`、支付宝 `alipay.jpg`（配置路径同步从 `alipay.png` 改为 `alipay.jpg`）。
- 删除 4 个测试动态：`src/content/dynamic/` 下全部 `.md`。
- 删除广告图 `public/assets/images/ad/ad1.webp`、旧赞赏码 `public/assets/images/sponsor/alipay.png`。
- 同步 `.gitignore`：将 `GitHub_Pages构建源修复.md` 加入忽略（该文档仅供本地排错参考，不进仓库）。
- 部署仓库实际工作副本位于 `E:/Coding/ghio_clone`（与线上 7.2.3 模板一致）；`E:/Coding` 下的 `Firefly` 为本地参考副本。
